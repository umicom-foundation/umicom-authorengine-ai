# ------------------------------------------------------------------------------
# Umicom AuthorEngine AI - Git attributes
# Normalize line endings and keep shell scripts with LF so they run on Unix/macOS.
# Keep PowerShell scripts CRLF to play nicely with Windows host editors.
# ------------------------------------------------------------------------------

# Text by default, auto-normalize
* text=auto

# Source (prefer LF)
*.c      text eol=lf
*.h      text eol=lf
*.md     text eol=lf
*.txt    text eol=lf
*.yml    text eol=lf
*.yaml   text eol=lf
*.json   text eol=lf

# Windows scripts prefer CRLF
*.ps1    text eol=crlf
*.bat    text eol=crlf
*.cmd    text eol=crlf

# Unix shell scripts prefer LF
*.sh     text eol=lf
*.bash   text eol=lf

# Make sure shell scripts are seen as executable on Unix/macOS clones
scripts/*.sh            eol=lf
scripts/run-uaengine.sh eol=lf

# Keep GitHub Actions YAML with LF
.github/*.yml  text eol=lf
.github/*.yaml text eol=lf
