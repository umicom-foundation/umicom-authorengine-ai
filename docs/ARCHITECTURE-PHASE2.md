# Phase 2 — Loosely Coupled Layout (Incremental, Non‑Breaking)

This phase adds a provider abstraction for LLMs and keeps the default build
exactly as before. You can enable providers opt‑in via CMake flags.
